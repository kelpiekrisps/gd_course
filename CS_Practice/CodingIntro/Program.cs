using System;

public class Program {
    public static void Main() {
        // this makes it easier to avoid mistakes
        // weapon must be an enum representing a weapon
        Weapon wp = Weapon.Sword;
    }
}